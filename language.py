class Language():
    def __init__(self,le):
        self.lll =1
        self.le = le
    
    def lumkof(self):
        if self.le == True:
            return "打开原始文件"
        else:
            return "Открыть исходный файл"
    def lumkff(self):
        if self.le == True:
            return "创建原始文件"
        else:
            return "Создать исходный файл"
    def lumkcj(self):
        if self.le == True:
            return "创建加密文件"
        else:
            return "Создавать зашифрованные файлы"
    def lumkef(self):
        if self.le == True:
            return "打开加密文件"
        else:
            return "Открыть зашифрованные файлы"
    def lumkfl(self):
        if self.le == True:
            return "打开密钥文件"
        else:
            return "Открыть ключевой файл"
    def lumkll(self):
        if self.le == True:
            return "创建密钥文件"
        else:
            return "Создать ключевой файл"
    def lustjm(self,):
        if self.le == True:
            return "开始加密"
        else:
            return "начать шифрование"
    def lustxm(self):
        if self.le == True:
            return "开始解密"
        else:
            return "начать расшифровку"
    def lute(self):
        if self.le == True:
            return "在这里输入文件内容"
        else:
            return "Введите содержимое файла здесь"
    def lutx(self):
        if self.le == True:
            return "文件在这里输出"
        else:
            return "вывод файла здесь"
    def lumkcc(self):
        if self.le == True:
            return "打开文件"
        else:
            return "открыть файл"
    def lumkxc(self):
        if self.le == True:
            return "保存文件"
        else:
            return "сохранить документ"